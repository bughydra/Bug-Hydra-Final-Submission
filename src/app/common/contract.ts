export class Contract {

  constructor(public contractSpecialId: string,
              public contractType: string,
              public paymentType: string,
              public startDate: string,
              public endDate: string) {
  }
}
